apiVersion: v2
name: symfony-app-chart
description: Symfony / nginx+php-fpm symfony-app — app + migrate job, with optional Messenger worker and scheduler
type: application
version: 0.1.0
appVersion: "1.0.0"
